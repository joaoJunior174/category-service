CREATE TABLE tb_category (
    session_token VARCHAR(255),
    sku VARCHAR(255),
    level1 VARCHAR(255),
    level2 VARCHAR(255),
    level3 VARCHAR(255),
    level4 VARCHAR(255),
    category_path VARCHAR(255),
    category_to VARCHAR(255),
    remove_from VARCHAR(255)
);